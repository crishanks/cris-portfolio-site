import React, { Component } from "react";
import { Container } from '../../theme/grid';

class Projects extends Component {
    render() {
        return (
            <Container>
                <h1>React-Zoomy no longer available.</h1>
            </Container>
        );
    }
}

export default Projects;